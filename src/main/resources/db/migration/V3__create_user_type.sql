CREATE TABLE auth.user_type (
                                id UUID PRIMARY KEY,
                                name VARCHAR(50) NOT NULL,
                                description VARCHAR(124)
);
